/**
 * @deprecated Import from '@/lib/api-validation' instead.
 * Kept for backward compatibility with existing API routes.
 */
export {
  parseIntSafe,
  requireString,
  requirePositiveInt,
} from './api-validation';
